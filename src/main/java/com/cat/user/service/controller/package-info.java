/**
 * HTTP adapters: REST controllers, API version constants, request mapping only.
 * Must not contain persistence or domain logic (Constitution Principle I).
 */
package com.cat.user.service.controller;
